object Test {
    fun sum(a: Int, b: Int): Int {
        return a * b
    }

    fun product(a: Int, b: Int): Int {
        return a * b
    }
}

fun main() {
    // Calculate the sum of two numbers
    println(Test.sum(1, 2))
Das

    asd
     as
            da
    // Calculate the product of two numbers
    println(Test.product(3, 4))
}